---
// Bloque "cómo evoluciona el precio de la luz": media diaria, tendencia y gráfica.
// Se calcula en el build a partir de src/data/historico.json. La única pieza de cliente
// es el hover del propio GraficaLinea (ver ese componente).
import { analizar, veredicto } from '../lib/tendencia.js';
import GraficaLinea from './GraficaLinea.astro';

interface Props {
  fechas: string[];
  media: (number | null)[];
  min: (number | null)[];
  max: (number | null)[];
}

const { fechas, media, min, max } = Astro.props;

const a = analizar(media);
const v = veredicto(a);

const fmtCorta = (f: string) => new Date(f + 'T12:00:00').toLocaleDateString('es-ES', { day: 'numeric', month: 'short' });
const serie = fechas
  .map((fecha, i) => ({ fecha, valor: media[i], etiqueta: fmtCorta(fecha) }))
  .filter((p): p is { fecha: string; valor: number; etiqueta: string } => typeof p.valor === 'number');

const cent = (n: number) => (n * 100).toLocaleString('es-ES', { maximumFractionDigits: 1 });
const eur = (n: number) => n.toLocaleString('es-ES', { minimumFractionDigits: 4, maximumFractionDigits: 4 });

const COLOR = { rojo: 'var(--color-caro)', verde: 'var(--color-acento)', neutro: 'var(--color-medio)' } as Record<string, string>;
const colorV = v ? COLOR[v.color] : 'var(--color-medio)';

const dias = a?.dias ?? 0;
const fmtFecha = (f: string) =>
  new Date(f + 'T12:00:00').toLocaleDateString('es-ES', { day: 'numeric', month: 'short' });
const desde = fechas.length ? fmtFecha(fechas[0]) : '';
const hasta = fechas.length ? fmtFecha(fechas[fechas.length - 1]) : '';

// Mínimo y máximo de la serie de medias, con su fecha, para el pie de la gráfica.
const validos = media
  .map((valor, i) => ({ valor, fecha: fechas[i] }))
  .filter((p): p is { valor: number; fecha: string } => typeof p.valor === 'number');
const diaBarato = validos.reduce((min, p) => (p.valor < min.valor ? p : min), validos[0]);
const diaCaro = validos.reduce((max, p) => (p.valor > max.valor ? p : max), validos[0]);
---
{v && a && (
  <section class="px-4 pt-8">
    <h2 class="text-base font-bold text-tinta">Cómo evoluciona el precio de la luz</h2>
    <p class="mt-1 text-xs text-tenue">Media diaria del PVPC · del {desde} al {hasta} · pasa el ratón o el dedo por la curva</p>

    <div class="mt-3 rounded-2xl border border-borde bg-superficie p-4">
      <div class="flex items-start gap-3">
        <span class="mt-0.5 h-10 w-1 shrink-0 rounded-full" style={`background-color:${colorV}`}></span>
        <div>
          <p class="text-base font-bold" style={`color:${colorV}`}>{v.titulo}</p>
          <p class="mt-1 text-sm leading-relaxed text-suave">{v.texto}</p>
        </div>
      </div>

      <div class="mt-4 flex flex-wrap items-baseline gap-x-6 gap-y-1">
        <div>
          <p class="text-xs font-semibold uppercase tracking-wide text-tenue">Media de hoy</p>
          <p class="mt-1 font-mono text-2xl font-bold text-tinta">
            {cent(a.actual as number)} <span class="text-sm font-semibold text-tenue">cent/kWh</span>
          </p>
        </div>
        <p class="text-xs text-suave">
          Media de los últimos {dias} días: <strong class="text-tinta">{cent(a.media)} cent/kWh</strong>
        </p>
      </div>

      <div class="mt-4">
        <GraficaLinea id="tendencia-diaria" serie={serie} unidad="cent/kWh" colorMinEsBueno={true} />
      </div>

      <div class="mt-3 flex flex-wrap items-center gap-x-5 gap-y-1 border-t border-borde pt-3 text-xs text-suave">
        <span>Día más barato: <strong class="text-acento">{cent(diaBarato.valor)} cent</strong> ({fmtFecha(diaBarato.fecha)})</span>
        <span>Día más caro: <strong class="text-caro">{cent(diaCaro.valor)} cent</strong> ({fmtFecha(diaCaro.fecha)})</span>
        <a href="/precio-medio-mensual/" class="ml-auto font-semibold text-acento hover:underline">Ver medias por mes →</a>
      </div>

      <details class="mt-3 border-t border-borde pt-3">
        <summary class="cursor-pointer text-xs font-semibold text-suave hover:text-acento">Ver los datos en una tabla</summary>
        <div class="mt-2 max-h-64 overflow-y-auto rounded-lg border border-borde">
          <table class="w-full text-left text-xs">
            <thead class="sticky top-0 bg-superficie text-tenue">
              <tr><th class="px-3 py-1.5 font-medium">Día</th><th class="px-3 py-1.5 font-medium">Media (cent/kWh)</th></tr>
            </thead>
            <tbody class="divide-y divide-borde">
              {[...serie].reverse().map((p) => (
                <tr>
                  <td class="px-3 py-1.5 text-suave">{p.etiqueta}</td>
                  <td class="px-3 py-1.5 font-mono text-tinta">{cent(p.valor)}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </details>

      <p class="mt-3 text-[11px] leading-relaxed text-tenue">
        Media diaria del PVPC en la Península, del {desde} al {hasta}, sin impuestos. Es la evolución
        real publicada por Red Eléctrica, no una predicción: nadie puede saber qué hará el precio
        mañana. Precios en céntimos de euro por kWh.
      </p>
    </div>
  </section>
)}
